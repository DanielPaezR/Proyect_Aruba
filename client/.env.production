# Cargado automaticamente por Vite en "vite build" (modo production).
# Sin sufijo /api: client/src/api/client.ts arma `${VITE_API_URL}/api/...` solo.
VITE_API_URL=https://arubaproyect-production.up.railway.app

# Clave publica VAPID — no es secreta, pero debe coincidir EXACTAMENTE con
# VAPID_PUBLIC_KEY configurada en el servicio de Railway del backend, o las
# suscripciones que arme el cliente no van a servir para enviarles push.
VITE_VAPID_PUBLIC_KEY=BB0UEd2OZUS9rwTpEIIA4w0oRmR81Pm7P0qJjYAsdU0FDsqZmWs7hM_THv8H-XStwdEatbykqwdMSlLww8Wn-sA
